// Re-export all vendor configs from the centralized location
// This file is kept for backward compatibility

export { 
  producerConfig, 
  restaurateurConfig, 
  transformerConfig, 
  transporterConfig, 
  exporterConfig 
} from '../../config/vendor';
